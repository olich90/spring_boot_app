server:
  port: 8080
  servlet:
    context-path: /app
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:spring_data_db;MODE=PostgreSQL
    username: admin
    password: admin
  jpa:
    show-sql: true
  h2:
    console:
      enabled: true
